{
  "name": "morris-pools-review-texts",
  "version": "1.0.0",
  "description": "Send review-request text messages to Morris Pools clients after jobs are completed",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
